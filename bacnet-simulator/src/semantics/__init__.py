"""Brick Core semantic model: entities/relationships built on top of the
legacy flat equipment_type/point_type/kind tags, additive and optional.

See src/legacy.py's semantic_entities/semantic_relationships tables (in
Database.setup()) for the persistence layer and CRUD methods, and CLAUDE.md
("Semantic model / Brick") for the migration rationale.
"""
