[pytest]
markers =
    core: fast regression tests that run in CI on every relevant change
    extended: slower sweeps/stability/regression tests; run manually or via workflow_dispatch
    integration: multi-FMU system-level co-simulation tests (e.g. RTU+VAV+ThermalZone); slower, run manually or via workflow_dispatch
