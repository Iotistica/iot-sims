"""Functional Test Builder — persisted BAS commissioning-test definitions
(devices/objects unrelated, project-level data). V1 is definition +
validation + persistence only; no execution engine yet — see
src/functional_tests/validation.py's own docstring for the exact
frontend/backend validation boundary."""
