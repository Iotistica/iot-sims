name: Build RTU FMU

on:
  push:
    branches:
      - main
      - feature/**
    paths:
      - bacnet-simulator/src/model_runtime/models/rtu/RTU.mo
      - bacnet-simulator/src/model_runtime/models/rtu/model.json
      - bacnet-simulator/src/model_runtime/models/rtu/tests/**
      - bacnet-simulator/src/model_runtime/shared/build-fmu.sh
      - bacnet-simulator/src/model_runtime/shared/validate_fmu_metadata.py
      - bacnet-simulator/Dockerfile
      - .github/workflows/build-rtu-fmu.yml
  workflow_dispatch:
    inputs:
      run_extended:
        description: "Also run the extended RTU test suite (slower sweeps/stability/step-response tests)"
        type: boolean
        default: false

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'
  MODELICA_BUILD_IMAGE: iotistic/modelica-build:om-1.27.0-buildings-13.0.0

concurrency:
  group: rtu-fmu-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  build:
    name: Build RTU.fmu and run regression tests
    runs-on: ubuntu-24.04
    defaults:
      run:
        working-directory: bacnet-simulator/src/model_runtime

    steps:
      - name: Checkout repository
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          fetch-depth: 0

      - name: Wait for Docker daemon
        run: |
          for i in $(seq 1 15); do
            docker info > /dev/null 2>&1 && echo "Docker daemon ready" && break
            echo "Waiting for Docker daemon... ($i/15)"
            sleep 2
          done
          docker info

      - name: Build RTU FMU
        run: |
          docker run --rm \
            -v "$PWD:/work" \
            -w /work \
            "$MODELICA_BUILD_IMAGE" \
            sh -c "mkdir -p /fmu-out && cp models/rtu/RTU.mo /build/RTU.mo && MODELICA_FILE=RTU.mo MODELICA_MODEL=RTU FMU_OUTPUT_NAME=RTU.fmu sh shared/build-fmu.sh && cp /fmu-out/RTU.fmu models/rtu/RTU.fmu"

      - name: Validate & sync RTU FMU metadata
        run: |
          python shared/validate_fmu_metadata.py \
            --fmu models/rtu/RTU.fmu \
            --metadata models/rtu/model.json \
            --fix

      - name: Build test runtime image
        working-directory: bacnet-simulator
        run: docker build --target model-runtime --pull --progress=plain -t model-runtime:ci -f Dockerfile .

      - name: Run RTU core regression tests
        working-directory: bacnet-simulator
        run: |
          set -o pipefail
          docker run --rm -v "$PWD/src/model_runtime:/app/model_runtime" -w /app/model_runtime model-runtime:ci \
            sh -c "pip install --quiet pytest && python -m pytest models/rtu/tests -m core -v" \
            | tee rtu-core-test.log

      - name: Run RTU extended regression tests
        if: github.event_name == 'workflow_dispatch' && inputs.run_extended == true
        working-directory: bacnet-simulator
        run: |
          set -o pipefail
          docker run --rm -v "$PWD/src/model_runtime:/app/model_runtime" -w /app/model_runtime model-runtime:ci \
            sh -c "pip install --quiet pytest && python -m pytest models/rtu/tests -m extended -v" \
            | tee rtu-extended-test.log

      - name: Write test summary
        if: always()
        working-directory: bacnet-simulator
        run: |
          {
            echo "# RTU Regression Tests"
            echo
            echo "| Item | Value |"
            echo "|---|---|"
            echo "| Model | RTU |"
            echo "| FMU | model_runtime/models/rtu/RTU.fmu |"
            echo "| FMI mode | Model Exchange |"
            echo "| Test suite | Core$([ '${{ github.event_name }}' = 'workflow_dispatch' ] && [ '${{ inputs.run_extended }}' = 'true' ] && echo ' + Extended') |"
            if [ -f rtu-core-test.log ] && tail -n 5 rtu-core-test.log | grep -q "passed"; then
              echo "| Result | PASS |"
            else
              echo "| Result | FAIL |"
            fi
            echo
            echo "## Core suite"
            echo '```'
            tail -n 15 rtu-core-test.log 2>/dev/null || echo "(no log captured)"
            echo '```'
            if [ -f rtu-extended-test.log ]; then
              echo
              echo "## Extended suite"
              echo '```'
              tail -n 15 rtu-extended-test.log
              echo '```'
            fi
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Commit generated RTU FMU
        run: |
          git add models/rtu/RTU.fmu models/rtu/model.json

          if git diff --cached --quiet -- models/rtu/RTU.fmu models/rtu/model.json; then
            echo "No RTU FMU changes to commit."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "Build RTU FMU [skip ci]"

          # This repo has several other FMU-build workflows that also
          # commit-and-push their own model's FMU back to the same branch,
          # several sharing trigger paths (shared/build-fmu.sh,
          # shared/validate_fmu_metadata.py) with this one -- a push
          # touching one of those files fires multiple such jobs
          # concurrently, and only the first to push wins; the rest see
          # "fetch first" since origin/main moved under them. Retry with a
          # rebase rather than fail outright: the conflicting commits touch
          # different files (different models' FMUs), so a rebase applies
          # cleanly in the normal case. (Same fix already applied to
          # build-ahu-v2-fmu.yml and build-boiler-fmu.yml after hitting this
          # exact race in practice.)
          for attempt in 1 2 3 4 5; do
            if git push; then
              echo "Pushed on attempt $attempt."
              exit 0
            fi
            echo "Push rejected (attempt $attempt/5) -- fetching and rebasing onto origin/$GITHUB_REF_NAME."
            git fetch origin "$GITHUB_REF_NAME"
            git rebase "origin/$GITHUB_REF_NAME"
            sleep $((attempt * 5))
          done

          echo "::error::Failed to push models/rtu/RTU.fmu and model.json after 5 attempts."
          exit 1
